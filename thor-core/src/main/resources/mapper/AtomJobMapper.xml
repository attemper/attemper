<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.thor.core.dao.mapper.AtomJobMapper">

    <select id="get" parameterType="map" resultType="com.thor.sdk.common.result.job.atom.AtomJob">
        select * from ${tablePrefix}job j
        <where>
            <include refid="condJobName"/>
            <include refid="condTenantId"/>
        </where>
    </select>

    <select id="list" parameterType="map" resultType="com.thor.sdk.common.result.job.atom.AtomJob">
        select *
        from ${tablePrefix}job j
        <where>
            <if test="jobName != null">
                and JOB_NAME like concat('%', #{jobName, jdbcType=VARCHAR}, '%')
            </if>
            <if test="jobType != null">
                and JOB_TYPE = #{jobType, jdbcType=INTEGER}
            </if>
            <if test="displayName != null">
                and `DISPLAY_NAME` like concat('%', #{displayName, jdbcType=VARCHAR}, '%')
            </if>
            <include refid="condTenantId"/>
        </where>
        <choose>
            <when test="sort == null">
                order by JOB_NAME
            </when>
            <otherwise>
                order by ${sort}
            </otherwise>
        </choose>
    </select>

    <delete id="delete" parameterType="map">
        delete from ${tablePrefix}job
        <where>
            <if test="jobNames != null">
                JOB_NAME in
                <foreach item="jobName" collection="jobNames" open="(" separator="," close=")">
                    #{jobName}
                </foreach>
            </if>
            <if test="tenantId != null">
                and TENANT_ID = #{tenantId, jdbcType=VARCHAR}
            </if>
        </where>
    </delete>

    <sql id="condJobName">
        <if test="jobName != null">
            and j.JOB_NAME = #{jobName, jdbcType=VARCHAR}
        </if>
    </sql>

    <sql id="condTenantId">
        <if test="tenantId != null">
            and j.TENANT_ID = #{tenantId, jdbcType=VARCHAR}
        </if>
    </sql>
</mapper>